{
  "name": "johnson-web-tools",
  "private": true,
  "workspaces": ["packages/shared", "packages/rewards", "packages/tools"],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}
